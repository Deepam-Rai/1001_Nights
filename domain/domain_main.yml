version: "3.1"

intents:
  - greet
  - goodbye
  - affirm
  - deny
  - bot_challenge

actions:
  - action_handle_fallback
  - action_greet

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
